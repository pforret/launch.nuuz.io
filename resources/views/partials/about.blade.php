<section class="page-section bg-primary" id="{{$block_id}}">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <h2 class="text-white mt-0">{{$block_title}}</h2>
                <hr class="divider light my-4" />
                <p class="text-white-50 mb-4">{{$block_text}}</p>
                <a class="btn btn-light btn-xl js-scroll-trigger" href="{{$button_url}}">{{$button_text}}</a>
            </div>
        </div>
    </div>
</section>
